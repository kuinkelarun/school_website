# ============================================================
# LOCAL DEVELOPMENT ENVIRONMENT TEMPLATE
# ============================================================
# Copy this file to .env.local and restart the dev server:
#
#   cp .env.local.example .env.local   (Mac/Linux)
#   copy .env.local.example .env.local  (Windows)
#
# .env.local is gitignored — this template file is committed
# so every developer gets the same local setup.
# ============================================================

# ─── Firebase Client SDK ─────────────────────────────────────
# Only needed when NEXT_PUBLIC_BYPASS_AUTH=false (real Firebase).
# Get values from Firebase Console > Project Settings > General.
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# ─── Firebase Admin SDK (optional, server-side only) ─────────
FIREBASE_ADMIN_CLIENT_EMAIL=firebase-adminsdk@your_project_id.iam.gserviceaccount.com
FIREBASE_ADMIN_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nyour_private_key_here\n-----END PRIVATE KEY-----\n"

# ─── App URL ─────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ─── Firebase Emulators ──────────────────────────────────────
# Set true only when running: firebase emulators:start
# Keep false when using NEXT_PUBLIC_BYPASS_AUTH=true
NEXT_PUBLIC_USE_EMULATORS=false

# ─── Seed script credentials ─────────────────────────────────
# Used by:  node scripts/seed-emulator.js
# Only relevant when using Firebase emulators.
SEED_ADMIN_EMAIL=admin@school.com
SEED_ADMIN_PASSWORD=changeme123

# ─── Bypass Auth (recommended for local UI testing) ──────────
# When true, Firebase is skipped completely.
# - Log in at /login with the email/password below
# - All data (announcements, events, hero images, etc.)
#   is stored in the browser's localStorage
# - No emulators, no Firebase project needed
#
# To disable and use real Firebase, set this to false and
# fill in the Firebase SDK values above.
NEXT_PUBLIC_BYPASS_AUTH=true
NEXT_PUBLIC_BYPASS_ADMIN_EMAIL=admin@school.com
NEXT_PUBLIC_BYPASS_ADMIN_PASSWORD=changeme123
